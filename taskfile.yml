version: '3'

tasks:
  init:
    description: "Install the required dependencies for the project"
    cmds:
      - brew install kerren/brrelease-tap/brrelease

  release:
    description: "Runs a release using semantic versioning"
    cmds:
      - brrelease release --merge-into-branch=main
